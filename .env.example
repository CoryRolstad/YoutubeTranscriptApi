# ============================================
# API Key Configuration
# ============================================

# Option 1: Direct environment variable (for local development)
# Set API_KEY to require authentication on all endpoints
# API_KEY=your_secret_key_here

# Option 2: File-based secrets (RECOMMENDED for production)
# Use API_KEY_FILE to point to a file containing the API key
# This is more secure and follows Docker secrets best practices
# API_KEY_FILE=/run/secrets/api_key

# IMPORTANT: API_KEY and API_KEY_FILE are mutually exclusive
# Setting both will cause an error on container startup
